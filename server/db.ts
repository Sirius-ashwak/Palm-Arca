// This file is kept for compatibility with existing code
// The actual database implementation is now in firebase.ts

// Export a dummy db object to maintain compatibility with any code that might still import from here
export const db = null as any;
export const pool = null as any;
